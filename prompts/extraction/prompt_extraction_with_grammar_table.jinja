You are an expert Luxembourgish–English translator and linguist.

You will be provided with one specific row from a grammar table that contains information about Luxembourgish grammar, vocabulary, or usage, along with surrounding context from the table. The table may contain errors or unclear formatting.

Your task is to:
1. Identify and extract all core grammar points from the passage.
2. For each grammar point, return examples that appear in the passage.
3. If no examples appear in the passage, create at least three English–Luxembourgish sentence pairs illustrating this rule.
4. If the passage lacks clear grammar points, provide standard examples related to the topic.
5. Example pairs must follow this format: (English sentence. Luxembourgish sentence).
6. For each grammar point, provide:
   - grammar_X: a short, concise description of the rule (with X replaced by the correct number).
   - description: a detailed explanation of this grammar rule with clarifications.
   - examples: a list of bilingual examples (from the passage or created).

Strict length and quality requirements for examples:
- Each example pair must contain two full sentences: one in English and one in Luxembourgish.
- Each sentence must be at least {{ min_words_per_sentence | default(20) }} words long; aim for {{ target_min_words | default(22) }}–{{ target_max_words | default(28) }} words to ensure richness and sufficient grammatical context.
- If a sentence from the passage is shorter than {{ min_words_per_sentence | default(20) }} words, expand it minimally while preserving its meaning, or replace it with a newly created sentence that satisfies the rule.
- Use varied vocabulary, tenses, and structures that directly demonstrate the grammar point (e.g., subordinate clauses, verb-second position, separable verbs, case marking).
- Avoid lists, fragments, ellipses, or bullet-like constructions; only full, natural sentences are allowed.
- Do not reuse identical sentences across different examples; paraphrase to ensure diversity.

The output must be strictly returned in valid JSON format, with no extra commentary:

{
  "grammars": [
    {
      "grammar_1": "string",
      "description": "string",
      "examples": [
        {
          "example_1_luxembourg": "string",
          "example_1_english": "string"
        },
        {
          "example_2_luxembourg": "string",
          "example_2_english": "string"
        }
      ]
    },
    {
      "grammar_2": "string",
      "description": "string",
      "examples": [
        {
          "example_1_luxembourg": "string",
          "example_1_english": "string"
        },
        {
          "example_2_luxembourg": "string",
          "example_2_english": "string"
        }
      ]
    }
  ]
}

Here is the complete HTML grammar table:
"""{{ GRAMMAR_TABLE }}"""

Here is the surrounding context:
"""{{ SURROUNDING_CONTEXT }}"""

Here is the specific row to analyze:
"""{{ SPECIFIC_ROW }}"""
